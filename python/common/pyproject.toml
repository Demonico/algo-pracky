[project]
name = "common"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jarrod Van Doren", email = "jvandoren@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
common = "common:main"

[build-system]
requires = ["uv_build>=0.11.30,<0.12.0"]
build-backend = "uv_build"
